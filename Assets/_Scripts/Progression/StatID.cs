// ============================================================================
// StatId.cs
// ----------------------------------------------------------------------------
// Minimal enum for primary attributes.
//
// We keep this separate so stat progression logic stays readable.
// ============================================================================


namespace UltimateDungeon.Progression
{
    public enum StatId
    {
        STR = 0,
        DEX = 1,
        INT = 2
    }
}
